<%if params[:answer]=="correct"%>
            <div class = "fade-green overlay animated fadeInUp delay-3s">
                <div class = "top-buffer-xlarge col-md-4 col-md-offset-4  ">
                    <div class="row bottom-buffer">
                        <h1 class ="center">You got it!</h1>
                    </div>
                    <form action=<%=rand(1..Question.all.count).to_s%>>
                        <div class = "row bottom-buffer">
                            <button type="submit" name ="answer" value = "wascorrect" 
                                    class ="red-background col-md-10 col-md-offset-1 btn btn-primary">
                            Next Question
                            </button>
                        </div>
                    </form>
                    <%if current_user.name != @question.creator%>
                        <%=link_to 'Upvote Question', rand(1..Question.all.count).to_s+'?answer=wascorrect',:action => increment_rep(@question.creator), class:"purple-background col-md-10 col-md-offset-1 btn btn-primary" %>
                    <%end%>
                </div>
            </div>
        <%end%>
        <%if params[:answer]=="wascorrect"%>
            <div class = "fade-green overlay animated fadeOutUp delay-3s">
                <div class = "top-buffer-xlarge col-md-4 col-md-offset-4 ">
                    <div class="row bottom-buffer">
                        <h1 class ="center ">You got it!</h1>
                    </div>
                    <form action=<%=rand(1..Question.all.count).to_s%>>
                        <div class = "row bottom-buffer">
                            <button type="submit" name ="answer" value = "wascorrect" 
                                    class ="red-background col-md-10 col-md-offset-1 btn btn-primary">
                            Next Question
                            </button>
                        </div>
                    </form>
                    <%if current_user.name != @question.creator%>
                        <%= link_to 'Upvote Question', rand(1..Question.all.count).to_s+'?answer=wascorrect', class:"purple-background col-md-10 col-md-offset-1 btn btn-primary" %>
                    <%end%>
                </div>
            </div>
        <%end%>
        <%if params[:answer]=="incorrect"%>
            <div class = "overlay fade-red animated fadeInUp delay-3s">
                <div class = "top-buffer-large col-md-4 col-md-offset-4 ">
                    <div class="row bottom-buffer">
                        <h1 class ="center ">:( that wasn't right</h1>
                    </div>
                    <form action=<%=rand(1..Question.all.count).to_s%>>
                        <div class = "row bottom-buffer">
                            <button type="submit" name ="answer" value = "wasincorrect" 
                                    class ="purple-background col-md-10 col-md-offset-1 btn btn-primary">
                            Next Question
                            </button>
                        </div>
                    </form>
                    <%if current_user.name != @question.creator%>
                        <%= link_to 'Upvote Question', rand(1..Question.all.count).to_s+'?answer=wasincorrect',:action => increment_rep(@question.creator), class:" green-background col-md-10 col-md-offset-1 btn btn-primary" %>
                    <%end%>
                </div>
            </div>
        <%end%>
        <%if params[:answer]=="wasincorrect"%>
            <div class = "overlay fade-red animated fadeOutUp delay-3s">
                <div class = "top-buffer-large col-md-4 col-md-offset-4  ">
                    <div class="row bottom-buffer">
                        <h1 class ="center ">:( that wasn't right</h1>
                    </div>  
                    <form action=<%=rand(1..Question.all.count).to_s%>>
                        <div class = "row bottom-buffer">
                            <button type="submit" name ="answer" value = "wasincorrect" 
                                    class ="purple-background col-md-10 col-md-offset-1 btn btn-primary">
                            Next Question
                            </button>
                        </div>
                    </form>
                    <%if current_user.name == @question.creator%>
                        <%= link_to 'Upvote Question', rand(1..Question.all.count).to_s+'?answer=wasincorrect', class:" green-background col-md-10 col-md-offset-1 btn btn-primary" %>
                    <%end%>
                </div>
            </div>
        <%end%>